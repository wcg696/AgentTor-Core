# ATP-1.0 技术规范 (Draft)

## 1. 协议定义
ATP (AgentTor Task Protocol) 是用于人形机器人任务调度的通用标准。

## 2. 核心模块定义
- **Vision**: 视觉识别模块，负责感知环境。
- **Logic**: 逻辑调度模块，负责路径规划。
- **Hardware**: 硬件执行模块，负责物理动作。

## 3. 标准参数
- `force`: 力度，单位为牛顿 (N)。
- `target`: 目标物体名称。
